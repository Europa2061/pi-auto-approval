# pi-auto-review

AI classifier based automatic approval extension for Pi coding agent.

The extension is disabled by default. Use `/auto-review fallback` for the recommended interactive mode, or `/auto-review auto` for unattended fail-closed mode.
